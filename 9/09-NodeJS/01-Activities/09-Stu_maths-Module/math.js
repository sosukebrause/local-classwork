module.exports = {
  sum: (x, y) => x + y,
  difference: (x, y) => x - y,
  product: (x, y) => x * y,
  quotient: (x, y) => x / y,
};

// module.exports = {
//   sum1: (a, b) => a + b,
//   diff: (a, b) => a - b,
//   prod: (a, b) => a * b,
//   power: (a, b) => a ** b,
// };
